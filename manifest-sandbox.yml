---
applications:
  - name: srrdigital-dev
    memory: 1G
    disk_quota: 2G
    env:
      CF_DOCKER_PASSWORD: ${CF_PASSWORD}
    docker:
      image: codesure/srrdigital:sandbox
      username: ${DOCKER_USERNAME}
